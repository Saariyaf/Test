// src/theme.js
import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#6200EE',  // Purple color
    },
    text: {
      primary: '#FFFFFF',  // White text
    },
  },
});

export default theme;
